# Core analysis
numpy>=2.0,<3
pandas>=2.2,<3
scikit-learn>=1.5,<2
matplotlib>=3.8,<4
seaborn>=0.13,<1
openpyxl>=3.1,<4

# API
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3

# Optional but recommended: XGBoost surrogate + Tree SHAP.
# The service falls back to RandomForest + exact six-feature SHAP when absent.
xgboost>=2.0,<4
shap>=0.46,<1
