# Comma-separated allowlist. Do not use * with authenticated production clients.
ALLOWED_ORIGINS=http://localhost:8501

# Used only when running src/analysis/dbscan_shap_batch.py offline.
ANALYSIS_INPUT_DIR=data/raw
ANALYSIS_OUTPUT_DIR=data/outputs

# Optional: one policy-report call per batch run. Keep secrets out of source control.
# ALAN_API_KEY=
# ALAN_API_URL=
